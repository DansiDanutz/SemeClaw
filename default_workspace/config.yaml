llm:
  provider: dashscope
  model: qwen3.6-plus
  api_key: "${DASHSCOPE_API_KEY}"
  temperature: 0.7
  max_tokens: 8192

default_agent: seme

# War Room settings
model: dashscope/qwen3.6-plus

telegram:
  bot_token: "${TELEGRAM_BOT_TOKEN}"
  chat_id: "${DLS_DAN_CHAT_ID}"
