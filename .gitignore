# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# Secrets — never commit these
.env
.env.*
*.env
!*.env.example
default_workspace/config.user.yaml
.env

# Generated state / logs
war_room/logs/
war_room/audio_cache/
war_room/*.json
war_room/*.json.lock

# OS
.DS_Store
Thumbs.db

# Logs
war_room/logs/*.log

# SemeClaw runtime instance ID — auto-generated, never commit
war_room/.instance_id

# SemeClaw v0.3.0 runtime state — NEVER commit (contains secrets + PII)
war_room/webhooks.json
war_room/shares.json
war_room/audio/_test.mp3
war_room/audio/meetings/*.mp3
war_room/audio/meetings/saved/*.mp3
war_room/audio/scripts/*.json
war_room/audio/segments/
war_room/tenants/
war_room/shared_state.json

# Rolling reports — examples only in repo; never commit live research content
war_room/research/test-ingest*.md
war_room/research/tenant-test*.md
war_room/research/bug-triage-*.md
war_room/voice_overrides.json

# Runtime KPI / state / task artifacts
war_room/task_meetings.json
war_room/task_queue.json
war_room/memory/memory.json

# Logs (all services)
logs/*.log
logs/*.jsonl
war_room/logs/*.jsonl

# Runtime research reports (real content — not for public repo)
# Keep demo/ reports (those are fixtures)
war_room/research/*.md
!war_room/research/saved/
war_room/builds/

# Pytest / ruff caches
.pytest_cache/
.ruff_cache/

# Launch config (local paths baked in)
.claude/launch.json

# Sentinel / coordinator runtime
sentinel/tripwire/
coordinator/*.log

# UPGRADE notes (internal)
UPGRADE_PLAN.md
