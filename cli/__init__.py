"""SemeClaw CLI — `semeclaw` command-line entry point.

Subcommands:
    semeclaw setup     interactive onboarding (API key, smoke test, save .env)
    semeclaw status    checklist of what's configured + what's missing
    semeclaw agents    list all registered agents (core + adapters)
    semeclaw demo      run the saved live-demo task (4 agents, real task)

Run:
    python -m cli              # same as `semeclaw`
    python -m cli setup
"""
